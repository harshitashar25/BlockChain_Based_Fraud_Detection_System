server:
  port: 8081

corda:
  rpc:
    host: localhost
    port: 10006         # party RPC port – set per node config
    username: user1
    password: test

ai:
  engine:
    url: http://localhost:5001

lea:
  backend:
    url: http://localhost:8080

logging:
  level:
    com.quadra: DEBUG
    net.corda: INFO

